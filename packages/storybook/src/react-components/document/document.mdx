import { Canvas, Controls, Meta } from "@storybook/blocks";
import * as DocumentStories from "./document.stories";

<Meta of={DocumentStories} />

# Document

Het Document-component plaatst een `<div>` met de class `utrecht-document` waaraan een aantal basis styles zijn gekoppeld die voor het hele document gelden, zoals de `font-family`.

## Playground

<Canvas of={DocumentStories.Playground} />
<Controls of={DocumentStories.Playground} />
