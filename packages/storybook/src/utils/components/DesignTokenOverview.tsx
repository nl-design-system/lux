import { type PropsWithChildren } from 'react';

const DesignTokenOverview = ({ children }: PropsWithChildren<void>) => (
  <section className="lux-theme--logius-light">{children}</section>
);

export { DesignTokenOverview };
